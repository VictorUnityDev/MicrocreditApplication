using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScreensScene : MonoBehaviour
{

    [SerializeField] private List<Screen> _screensForFirstStart;
    [SerializeField] private List<Screen> _screensForNextStart;
    

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
